#include <iostream>
#include "Monster.h"

using namespace std;

int main()
{
    //Monster newM;
    //Monster newM("Schwamm",12, 30, 50);
    // newM.NewMonster();
    //cout << newM.GetMonsterName() << "\t" << newM.GetArea() << "\t" << newM.GetBook()<< "\t" << newM.GetPage();
    //newM.SaveMonster();
    istream iFile;
    iFile.open("monsterlist.txt");
    if(iFile.is_open())
    {

    }
    return 0;
}
